"""Concrete IdentityProvider implementations (see ../provider.py).

One module per provider. Firebase is the only one today
(./firebase.py) - a future provider gets its own module here,
implementing the same IdentityProvider Protocol, without touching this
one.
"""
